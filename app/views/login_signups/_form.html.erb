<%= form_with(model: login_signup) do |form| %>
  <% if login_signup.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(login_signup.errors.count, "error") %> prohibited this login_signup from being saved:</h2>

      <ul>
        <% login_signup.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
